import React from 'react'

function Content ({datum}) {
  if(datum.food){console.log(datum)}
  if(datum.drink){console.log(datum)}
  if(datum.bizInfo){console.log(datum)}
  return(
    <div />
  )
}

export default Content